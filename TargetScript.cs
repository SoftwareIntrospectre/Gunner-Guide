using UnityEngine;
using System.Collections;

public class TargetScript : MonoBehaviour {

	//use WaitForSeconds function, maybe 3-5 seconds disable after destroyed.
	//disable renderer and collider, wait 3 seconds, then renable it

}
