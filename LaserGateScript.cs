using UnityEngine;
using System.Collections;

public class LaserGateScript : MonoBehaviour {

	//if target is hit: target is destroyed, associated Laser Gate is disabled


	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
